function solution(progresses, speeds) {
  var answer = [];
  let sum = 0;
  for (let i = 0; i < progresses.length; i++) {
    sum = progresses[i] + speeds[i];
    console.log("sum", sum);
  }
  return answer;
}

//푸는중입니다.....
